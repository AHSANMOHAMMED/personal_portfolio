# Deprecated: GitHub Pages static export
#
# This portfolio now targets Vercel so `/api/chat` can run as a serverless
# function. Deploy via the Vercel Git integration (or `vercel`) instead of
# uploading `./out` to GitHub Pages.
#
# Kept disabled to avoid broken Pages deploys after removing `output: 'export'`.

name: Deploy (disabled — use Vercel)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Reminder
        run: echo "Deploy this app on Vercel with GROQ_API_KEY set. GitHub Pages static export is no longer supported."
